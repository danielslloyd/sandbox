# Anthropic API Key for nutrition extraction (OPTIONAL - only if using --backend anthropic)
# By default, the scraper uses Ollama (free, local) which doesn't need an API key
# Get Anthropic API key at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=your_api_key_here
